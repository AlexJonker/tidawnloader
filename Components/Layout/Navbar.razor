@using System.Reflection
<div class="bg-neutral-950 border-b border-zinc-800">
  <div class="flex items-center justify-between px-4 h-14">
    <a class="text-white font-semibold tracking-tight text-sm" href="">Tidawnloader</a>
    <span class="text-zinc-500 text-xs">v@(Version)</span>
  </div>
</div>

@code {
  private string Version => Assembly.GetExecutingAssembly().GetName().Version?.ToString(3);
}